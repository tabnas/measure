name: Record historical measurement

on:
  workflow_dispatch:
  repository_dispatch:
    types: [parser-release]

permissions:
  contents: write

concurrency:
  group: record-measurement
  cancel-in-progress: false

jobs:
  record:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: main
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: npm
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.26.x'
          cache: true
      - name: Check the Rust toolchain
        # The ubuntu-24.04 image ships a stable Rust toolchain, so there is
        # no setup action to pin here. Printing the version puts the
        # compiler that produced the measurement in the log next to the Node
        # and Go versions, and fails loudly if a future image drops it.
        run: cargo --version && rustc --version
      - run: npm ci
      - run: GOWORK=off go mod download
      - run: cargo fetch --locked --manifest-path ports/rust/Cargo.toml
      - run: npm test
      - run: npm run measure
      - name: Commit immutable run
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results site/data/catalog.json site/data/history.json
          git commit -m "data: record Tabnas measurement"
          git push origin HEAD:main
