node_modules/
.build/
.DS_Store
coverage/
site/data/runs/
site/schemas/

# Vale's downloaded style packages. The vocabulary under
# .vale/styles/config/ and the house rule under .vale/styles/Tabnas/
# are ours and ARE committed; Google/ arrives from the pinned URL
# in .vale.ini via `vale sync`.
/.vale/styles/Google/

# Written by the counts script while measuring, removed after.
.vale-counts.ini

# The Rust runner keeps its build tree under .build/ like the other
# two ports, but `cargo build` run by hand from ports/rust/ would put one
# next to the manifest instead.
ports/rust/target/
